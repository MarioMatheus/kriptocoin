version: '3'
services:

  backend:
    build: ./backend
    container_name: backend
    ports:
      - 3001:3001

  frontend:
    build: ./frontend
    container_name: frontend
    ports:
      - 8081:8081
    links:
      - backend

