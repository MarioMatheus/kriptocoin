[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[scripts]
serve = "uvicorn app.main:app --reload"
test = "pytest"

[dev-packages]
pylint = "*"
pytest = "*"

[packages]
fastapi = "*"
uvicorn = "*"
requests = "*"

[requires]
python_version = "3.7"
